# Que: Variables de entorno para el backend
# Para que: Configurar la conexion a PostgreSQL y otros servicios
# Impacto: Sin este archivo el servidor no puede conectarse a la BD
# IMPORTANTE: Copiar como .env y NUNCA subir .env al repositorio

# ── BASE DE DATOS ─────────────────────────────────────────────
# Que: URL de conexion a PostgreSQL (formato Prisma)
# Para que: El backend sabe donde conectarse
# Impacto: Cambiar segun el entorno (local, staging, produccion)
DATABASE_URL="postgresql://cancha_user:cancha_password@localhost:5433/cancha_sintetica_db"

# ── SERVIDOR ──────────────────────────────────────────────────
PORT=3000
NODE_ENV=development

# ── AUTENTICACION ─────────────────────────────────────────────
# Que: Clave secreta para firmar tokens JWT
# Para que: Autenticar usuarios de forma segura
# Impacto: Cambiar por una clave fuerte y unica en produccion
JWT_SECRET=cambiar_por_clave_secreta_segura_en_produccion
JWT_EXPIRES_IN=7d
